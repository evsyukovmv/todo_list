%h2
  All task lists
  - if !@project.nil?
    of project
    = @project.name
#task_list
  = render('shared/task_lists_item')
- if !@project.nil?
  = link_to('New task list', new_project_task_list_path)
- else
  = link_to('New task list', new_task_list_path)